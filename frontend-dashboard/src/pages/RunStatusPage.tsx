export function RunStatusPage() {
  return (
    <div>
      <h1>Run status</h1>
      <div className="card">
        <p>Ingestion and analysis batch progress (pending / running / partial / failed).</p>
      </div>
    </div>
  );
}
