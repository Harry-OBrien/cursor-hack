/** TODO: POST to ingestion API to start ingest job */

export function BrandSetupPage() {
  return (
    <div>
      <h1>New brand</h1>
      <div className="card">
        <p>Form: name, primary domain, competitors, seed topics.</p>
        <p>Wire to ingestion CLI/API when Engineer 1 exposes create endpoint.</p>
      </div>
    </div>
  );
}
